class Circular {
  final String cId;
  final String startDate;
  final String endDate;
  final String title;
  final String description;
  final String tag;
  final String url;
  Circular(this.cId, this.startDate, this.endDate, this.description, this.title,
      this.tag, this.url);
}
